# Sets is the collecition of well define objects

s = set() # Empty Set Don't use s = {} as it will create an empty 

# Why we use set: Becauese it can not repeate any element 
# We use the set in that usecase that we wasen't reapete any value


d = {2, 1, 3, 1, 2, 4, 5}

print(d) # it can not reate the value


# Methods of sets

# add()

d.add(10)


# update()   Adds multiple elements from another iterable (list, tuple, set).


d.update()


# remove()

d.remove(10)


# discard()   Removes the element if present; no error if not found.

d.discard(10)


# pop()  Removes and returns an arbitrary element.

d.pop()

# clear() Removes all elements from the set.

d.remove()


#  copy  Returns a shallow copy of the set.

d.copy()


